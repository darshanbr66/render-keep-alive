name: Render Keep Alive

on:
  schedule:
    - cron: "3/10 * * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render services
        run: |
          curl --fail --silent --show-error --max-time 30 "${{ secrets.RENDER_URL }}"
